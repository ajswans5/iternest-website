# Journal Content

This folder contains source copy for **From the Nest**, the IterNest build journal.

Use `_template.md` for new entries. Change `status` from `draft` to `published` only when the post is approved for the live site.
